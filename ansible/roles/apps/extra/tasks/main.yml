---
- name: Extra | Install packages
  ansible.builtin.include_role:
    name: lib/packages
  vars:
    install_label: Extra
    install_pacman_packages: "{{ extra_pacman_packages }}"
    install_aur_packages: "{{ extra_aur_packages }}"
